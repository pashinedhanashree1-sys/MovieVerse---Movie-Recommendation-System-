# MovieVerse---Movie-Recommendation-System-
MovieVerse is a KNN-based movie recommendation system built using the MovieLens dataset. It uses collaborative filtering and cosine similarity to provide personalised movie recommendations based on user ratings and movie similarity.
